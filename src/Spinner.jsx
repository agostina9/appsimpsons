import './spinner.css'

const Spinner = () => {
    return (
        <section className='d-flex justify-content-center'>
            <div className="spinner"></div>

        </section>
    );
};

export default Spinner;